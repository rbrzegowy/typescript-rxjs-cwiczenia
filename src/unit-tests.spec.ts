import { afterEach, describe, expect, it, vi } from 'vitest';

// przydamisie:
// vi.useFakeTimers();
// vi.useRealTimers();
// vi.advanceTimersByTime();

describe('testy strumieni', () => {
  afterEach(() => {});

  it('odrzuca liczby nieparzyste i podwaja liczby parzyste', async () => {});
});
